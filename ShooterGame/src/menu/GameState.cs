namespace ShooterGame
{
    public enum GameState
    {
        MAIN_MENU,
        IN_GAME,
        PAUSED,
        LEVEL_SELECT,
        SETTINGS,
        GAME_OVER,
        WIN,
        INSTRUCTIONS
    }
}